# ViralCutz

## Overview
ViralCutz is a project designed to create viral video content leveraging various multimedia processing tools.

## Installation
Use the package manager [pip](https://pip.pypa.io/en/stable/) to install the required dependencies:

```bash
pip install -r requirements.txt
```